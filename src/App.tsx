import { FC } from "react";
import { Header } from "./common/componenets/header/Header";
import React from "react";


interface AppProps {}

export const App: FC<AppProps> = ({}) => {
  return (
      <Header />
  );
};
